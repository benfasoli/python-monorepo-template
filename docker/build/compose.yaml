services:
  api:
    image: api:latest
    build:
      context: ../../
      dockerfile: services/api/Dockerfile
    ports:
      - 8080:8080

  worker:
    image: worker:latest
    build:
      context: ../../
      dockerfile: services/worker/Dockerfile
