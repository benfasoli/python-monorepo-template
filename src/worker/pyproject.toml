[project]
name = "worker"
version = "0.0.0"
requires-python = "~=3.13.0"
dependencies = [
    "lib-core",
    "lib-dtos",
]

[tool.uv]
dev-dependencies = [
    "python-dotenv[cli] ~= 1.0",
]

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"
